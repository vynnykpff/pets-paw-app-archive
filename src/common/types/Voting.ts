// TODO: Check on correct fields
export interface Voting {
  sub_id: string;
  image_id: string;
  value: number;
}

export interface ReactionStatus {
  url: string;
  value: number;
}

export interface VotingImage {
  id: string;
  url: string;
}
